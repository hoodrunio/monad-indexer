version: '3.9'

services:
  visualizer:
    image: ghcr.io/blockscout/visualizer:latest
    pull_policy: always
    container_name: visualizer
    restart: always
    env_file:
      -  ./envs/common-visualizer.env
    ports:
      - 8081:8050
