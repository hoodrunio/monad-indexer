# ArgoCD Root Application for Staging Environment
# App-of-Apps Pattern - deploys only staging applications

apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: monad-indexer-root-staging
  namespace: argocd
  # Finalizers ensure proper cleanup
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default

  source:
    repoURL: https://github.com/hoodrunio/monad-indexer
    targetRevision: staging  # Staging uses staging branch
    path: argocd/applications/staging

  destination:
    server: https://kubernetes.default.svc
    namespace: argocd

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
