# ArgoCD Root Application for Production Environment
# App-of-Apps Pattern - deploys only production applications

apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: monad-indexer-root-production
  namespace: argocd
  # Finalizers ensure proper cleanup
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default

  source:
    repoURL: https://github.com/hoodrunio/monad-indexer
    targetRevision: stable  # Production uses stable branch
    path: argocd/applications/production

  destination:
    server: https://kubernetes.default.svc
    namespace: argocd

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
