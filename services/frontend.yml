version: '3.9'

services:
  frontend:
    image: ghcr.io/blockscout/frontend:latest
    pull_policy: always
    container_name: frontend
    restart: always
    env_file:
      -  ./envs/common-frontend.env
