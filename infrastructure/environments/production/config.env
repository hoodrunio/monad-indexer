# Production Environment Configuration
# This file is sourced by installation scripts

# Environment metadata
export ENVIRONMENT="production"
export NAMESPACE="monad-indexer-production"

# Network configuration
export PUBLIC_IP="65.109.80.202"
export IP_POOL_CIDR="65.109.80.202/32"

# Domains and services
# Format: DOMAIN_NAME|HOSTNAME|SERVICE_NAME|SERVICE_NAMESPACE|SERVICE_PORT|TLS_SECRET
export DOMAINS=(
  "argocd|cd.hoodscan.io|argocd-server|argocd|443|argocd-tls"
  "monad-indexer|monad-tn1-indexer.hoodscan.io|backend|monad-indexer-production|4000|monad-indexer-tls"
)

# Let's Encrypt configuration
export LETSENCRYPT_EMAIL="infra@hoodrun.io"
export LETSENCRYPT_ISSUER="letsencrypt-prod"  # or letsencrypt-staging for testing

# Gateway configuration
export GATEWAY_NAME="monad-indexer-gateway"
export GATEWAY_CLASS="cilium"
