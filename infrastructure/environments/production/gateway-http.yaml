# HTTP-only Gateway dedicated to ACME HTTP-01 challenges
# Shares the same public IP as the main Gateway (65.109.80.235)

apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: monad-indexer-http-gateway
  namespace: monad-indexer-production
  annotations:
    argocd.argoproj.io/sync-wave: "-0.5"
    io.cilium.lb/ipam-sharing-key: monad-indexer-public
    lbipam.cilium.io/sharing-key: monad-indexer-public
  labels:
    gateway: monad-indexer
    environment: production
spec:
  gatewayClassName: cilium

  addresses:
    - type: IPAddress
      value: "65.109.80.235"

  # Pass annotations to LoadBalancer Service for IP sharing
  infrastructure:
    annotations:
      io.cilium.lb/ipam-sharing-key: monad-indexer-public
      lbipam.cilium.io/sharing-key: monad-indexer-public

  listeners:
    - name: http
      protocol: HTTP
      port: 80
      allowedRoutes:
        namespaces:
          from: All  # Required for cert-manager HTTPRoutes
